# 更新日志

本文件记录**各个 skill 自己**的版本变更。**仓库本身不设版本号**——版本归属于单个 skill，以其 `SKILL.md` frontmatter 的 `version` 为准。仓库新增 skill 时，只在下方增加该 skill 的小节与它的版本条目，不改动已有 skill 的版本。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

文件分为两部分：各 skill 的版本变更（`## <skill 名>`），以及文件末尾的 [仓库（不设版本号）](#仓库不设版本号)（按日期记录脚手架、全局约定与跨 skill 改动）。

## prompt-mentor

### [0.5.1] - 2026-09-13

弹窗覆盖面补齐与文档结构修正：无选项的开放式追问也走弹窗，并修掉阶段文件里与「阶段/小节」交叉引用对不上的标题层级。

#### 新增

- **开放式追问也弹窗**（SKILL.md 第 5 节）：原先规定"工具只用于有离散选项的裁决，开放式追问仍走正文"，实测证明这是下一个漏点——DSH 客户端会把**省略 `options` 的问题**渲染成带输入框的块状区域（`QuestionComposer.tsx` 的 `variant="block"`）。现在无选项问题照样调用工具、只省略 `options` 字段，用户在弹窗里直接打字，不必回正文另写一条。

#### 修复

- **阶段 1 / 2 / 3 的标题层级**（stage1-clarify.md、stage2-probe-audit.md、stage3-consensus.md）：三个文件里第 1 节是 `##`，而第 2 节起却是 `###`，Markdown 会把它们渲染成第 1 节的下级，正文里"第 4 节""第 8 节"这类交叉引用因此对不上号（例如 stage1 第 4 节写着"见第 8 节模板"）。现已统一为同级 `##`。

#### 变更

- **回退格式的占位符**（SKILL.md 第 5 节）：`**推荐：<选项>**` 改为 `**推荐：<具体选项>**`，避免被读成要原样照抄 `<选项>` 四个字。

### [0.5.0] - 2026-09-13

交互方式补位：需要用户拍板的内容改用运行环境的交互式提问工具弹出可点选项，不再只在正文里列 A/B/C 让用户自己复制回话。

#### 新增

- **交互式提问规则**（SKILL.md 第 5 节）：环境提供交互式提问工具（DSH 的 `ask_user_question`、Claude Code 的 `AskUserQuestion` 等）时，四个拍板点——阶段 1 待确认项 / 设计建议 / 任务拆分确认、阶段 2 需求外不合理裁决、阶段 3 放行、阶段 4 回退确认——都必须弹出可点选项：推荐项放第一个并在标签末尾加 `(Recommended)`，推荐理由写进该选项的 `description`，选项标签与正文逐字一致，同一批问题一次调用问完。
- **交付模式决定放行推荐项**（stage3-consensus.md 第 4 节）：模式 A（默认）推荐 `开始执行`，模式 B 推荐 `确认`；第三个选项 `需要调整` 用于用户指出要改的条目。
- **Subagent 不得向用户提问**（SKILL.md 第 5 节、subagent-recon.md 第五与第七节）：Subagent 是主 Agent 的子代理，交互式提问对它不可用（DSH 以 `DELEGATED_CALLER` 拒绝，Claude Code 禁用 `AskUserQuestion`）。派发契约新增一条——未决问题写进返回结果的「探查范围与盲区」，由主 Agent 汇总后用一次调用统一弹出。

#### 变更

- **各阶段接入弹窗**（stage1-clarify.md 第 4 节、stage2-probe-audit.md 第 7 节、redesign-audit.md 第 3.5 节、stage3-consensus.md 第 4 节、stage4-execute.md 第 3 节、requirements-by-type.md）：正文选项、裁决表与放行请求照常输出，弹窗只是把同一批选项变成可点的；环境没有该工具时回退正文 `A. …　B. …` 加 `**推荐：<选项>** —— <一句理由>` 的格式，Skill 不假设运行环境一定具备交互式提问能力。
- **自检与迭代记录同步**（quality-checklist.md 基础项与交互反模式、templates/iteration-log.md）：基础项补三条交互自检，交互反模式补三条（有工具却只列文字选项 / 让 Subagent 去问用户 / 弹窗与正文选项不一致），迭代记录补一条诊断项。

### [0.4.1] - 2026-09-13

输出形态的自检补位，并把仓库脚手架内容移出 skill 版本条目。

#### 变更

- **迭代记录新增输出形态诊断项**（templates/iteration-log.md）：补三条——回复末尾又出现"教学 / 导师建议"板块（教学应内联在【优化后的需求描述】里）、【优化后的需求描述】又写成一整段、待确认项缺 AI 推荐项或没排在回复最末。

### [0.4.0] - 2026-09-13

按一次真实使用的反馈做的一轮输出形态改造：需求描述改为逐条结构化、待确认内容移到回复最末并强制给推荐、删除末尾独立教学板块。

#### 变更

- **【优化后的需求描述】改为逐条结构化**（optimized-requirement.md 第 2 节、stage1-clarify.md 第 2 与第 8 节）：先给"你的原始描述"原话，再给逐条需求块——一条需求一个块，块内一个要点一行（对象 / 触发 / 期望 / 边界 / 验收 / 否定式），必写 `对象`、`期望`、`验收`（Bug 类另加 `现状`）。禁止把一条需求写成一整段，或把多个条件用括号堆进一句话。
- **教学点内联，删除末尾教学板块**（optimized-requirement.md 第 3 节、SKILL.md 红线 10）：教学只发生在【优化后的需求描述】内部——结构性对照本身即教学，另加最多 2 条挂在对应需求块下的内联说明行（`措辞说明` / `写法肯定` / `可以套用`）。不再在回复末尾输出"教学：两处原话改写"这类板块。
- **待确认内容移到回复最末**（stage1-clarify.md 第 4 与第 8 节）：段落顺序固定为需求描述 → 需求清单 → 项目文档摘要 → 任务数 → 【需要确认】→【我的设计建议】；需要用户拍板的内容不再夹在中间，需求描述内部也不再内嵌"需要确认"。
- **每条待确认项与每条设计建议必须给 AI 推荐项**（stage1-clarify.md 第 4、5、8 节，requirements-by-type.md）：格式为 `**推荐：<选项>** —— <一句理由>`；确实无倾向时明说"两者皆可 —— 取决于……"，不得只列选项不给推荐。
- **自检与反模式同步**（quality-checklist.md）：教学项改为检查结构（逐条成块、必写三项、内联说明 ≤2 条、末尾无教学板块）；交互反模式补"只列选项不给推荐""待确认问题夹在中段"。

### [0.3.0] - 2026-09-13

在 0.2.0 四阶段流程基础上做的一轮可用性改造：区分交付模式、给小事开轻量通道、让验收真正独立，并把探测方式与契约边界补严。

#### 新增

- **两种交付模式**（SKILL.md 4.1）：模式 A「共识包 + 执行」与模式 B「只交付共识包」。模式 B 是正式交付形态——阶段 3 放行即锁定并结束流程，用户之后随时说"开始执行"即可继续；两种模式在阶段 1–3 行为完全一致。
- **轻量通道**（SKILL.md 4.2、stage1-clarify.md 第 6 节、stage3-consensus.md 第 2 节）：五条判据（单一目标 ≤5 条原子项 / 层一三必要条件全过 / 一轮内可锁定 / 非重设计 / 无迭代要求）全部满足时可走轻量通道：省略完整文档摘要、允许清单与简版共识包同轮给出、一次回复完成锁定与放行；内容为空的 `<verified_facts>`、`<unverified_assumptions>`、`<scope>`、`<non_goals>` 可整段省略。修正类与重设计类任务默认不走轻量通道。减少的是段落数与轮次，不是判据强度。
- **独立验收的 checker**（stage4-execute.md 第 4 节、parallel-execution.md 第七节）：满足任一条件（改动 ≥3 文件 / 含无法用测试或命令验证的否定式条件 / 修正类 / 用户要求）默认派 checker Subagent，输入只给 `<goal>` + `<acceptance_criteria>` + 仓库 + 改动清单，不给实现过程与修改理由——同一个 agent 无法把自己刚写的东西当陌生代码看。
- **语义改写标记**（stage1-clarify.md 第 6 节）：对用户原话做了收窄 / 扩大 / 合并 / 拆分 / 加约束 / 改范围的条目标注 `⚠ 已改写` 并在锁定前单独说明；存在未表态的改写条目不算锁定。
- **探测第五维度与"精准问用户"**（stage2-probe-audit.md 第 3 节、redesign-audit.md 第三节）：层二新增"待查事实数量"维度；待查事实 ≤2 条且属于用户在界面上可直接观察到的事实时，在阶段 1 追问里一次问清，不为此派 Subagent。
- **契约边界自足性**（consensus-package.md 第三节）：允许并鼓励模块 / 能力层级的约束措辞，把边界钉死而不指定实现位置。

#### 变更

- **放行话术拆成两个明确选项**（stage3-consensus.md 第 4 节）："确认"= 锁定不执行，"开始执行"= 锁定并进入阶段 4；放行方向由三个变为四个。
- **触发词与产物对齐**（SKILL.md frontmatter）：删除「提示词 / 帮我写prompt / prompt生成 / prompt导师」等指向"替我写一段文本"的触发词，改为「优化需求 / 怎么描述需求 / 需求澄清 / 需求共识 / 需求描述 / 共识包」。
- **回退细则归位**（stage4-execute.md 第 3 节）："回退时保留仍然有效的工作"由指针文件移回权威文件。
- **反模式清单加权威指针**（quality-checklist.md）：保留自检条目，补上各组主题的权威文件指向。

### [0.2.0] - 2026-09-13

从"跨窗口产出提示词"改为"同窗口产出共识包 + 角色切换执行"的四阶段流程，并按需读取拆分主文件。

#### 变更

- **产物由提示词改为共识包**：四阶段流程为需求澄清 → 探测与审计 → 共识包生成与放行 → 角色切换执行；产物是十段共识包（`<goal>`…`<uncertainties>`），不含 `<task>` 段落，不写具体文件路径、组件名、函数名。
- **边界更新**：删除"禁止读取源代码"的绝对边界，改为"可以读代码核实需求侧事实，但不用于决定实现方案"；阶段 1–3 不改项目，阶段 4 才可改。
- **阶段 2 改为两层决策**：层一三个必要条件判断是否需要探测，层二四个维度判断由谁执行（AI 直读 / Subagent，总数 2–4）；Subagent 只收集事实、给证据、标盲区，不给建议。
- **阶段 4 引入角色切换**：明确宣告切换为资深软件工程师，按 7 步执行；发现共识包问题按回退表回退，不允许自行改写共识包。
- **按需读取拆分**：`SKILL.md` 由 1091 行降到 277 行，只保留身份、边界、流程骨架与路由表；阶段规则移入 `references/stage1-clarify.md`、`stage2-probe-audit.md`、`stage3-consensus.md`、`stage4-execute.md`、`optimized-requirement.md`、`iteration-mode.md`。每条规则全库唯一权威，其余位置改为指针。
- `references/teaching-methods.md` 删除，内容并入主文件教学小节与 `references/optimized-requirement.md`。

#### 修复

- 清理全库残留的跨窗口表述（"复制到另一个窗口""新窗口"等），修正 `references/prompt-template.md` 中的示例文件路径与组件名，删除末尾"导师建议"板块。

### [0.1.0] - 2026-09-11

prompt-mentor 首个版本。基于一次真实使用（为用户本地知识库桌面应用生成「预览交互修复」与「文件多选右键菜单重新设计」两份提示词，并迭代两轮）暴露的 9 个问题完成首轮改造。

#### 新增

- **重设计类任务的强制前置**（SKILL.md 4.2、requirements-by-type.md 第 10 节、新增 `references/redesign-audit.md`）
  重新设计已有功能时，未取得现状清单不得生成最终提示词；拿到清单后逐项做三查——对称性（同类对象之间一边有一边没有的项）、归属（作用对象与所在层级是否匹配）、冲突（新需求与现有项是否重复或语义重叠）；发现不合理必须交用户裁决，不得默认保留、不得自行决定、不得原样带进新设计。
- **执行侧盘点与审计段落**：`<current_state_inventory>` 与 `<audit_findings_required>`，要求执行 Agent 先交付现状盘点表、把发现的不合理逐条上报，在确认前不得自行移除或合并。
- **功能类文档事实的处置规则**（SKILL.md 2.5）：文档描述的功能要成为设计内容时，必须走「来源 + 可信度 + 处置」三段式，未经验证不得作为「该功能已存在」的依据；用户口述与文档冲突时必须停下核对。
- **功能原子性规则**（SKILL.md 7.2、prompt-template.md 写作规范 8）：每个用户可感知功能独立成条，禁止把相邻功能隐式合并成一个入口；确需合并必须作为设计决策单列。
- **口头意图的防分叉翻译**（SKILL.md 7.2、prompt-template.md 写作规范 10）：用户说「复用 X 那一套」时，必须译成「复用同一实现 + 禁止平行副本 + 允许的最小适配范围 + 原调用方零变化」四件套。
- **否定式完成条件**（SKILL.md 7.0）：对「不应存在的东西」写「不存在 X，包括形式 A/B/C 与位置 L1/L2」，与肯定式条件并列使用。
- **修正类任务三段结构**（SKILL.md 7.4、prompt-template.md 修正类任务模板）：修正项 / 保持不动清单 / 否定式完成条件，并限定验证范围，禁止顺手修复清单外问题。
- **迭代模式的一致性自检**（SKILL.md 第 9 节、templates/iteration-log.md）：输出前核对本轮新增约束与已有约束是否冲突；冲突必须显式写例外条款并在修订说明中点明。

#### 变更

- **需求确认摘要强制二分**（SKILL.md 第 6 节）：拆成「你的需求与事实」与「我的设计建议」两节，导师自行发明的分组、交互形式、默认值、合并方案一律编号进后者供用户逐项否决，不得与需求事实混排。
- **教学模块改为原话精确化改写**（SKILL.md 第 8 节、重写 `references/teaching-methods.md`）：三种教学方法保留为内部选材逻辑，产出统一为「你的原话 / 问题出在（对象不具体·范围没限定·动作不可验证）/ 精确版」；禁止出现抽象方法论名词；条目数由素材决定，取消「必须 2-3 条」的硬指标。
- **质量清单与反模式同步扩充**：`references/quality-checklist.md` 新增 7 条检查项与 9 条反模式，`templates/iteration-log.md` 新增 4 条诊断项与「冲突与例外」记录节。
- SKILL.md frontmatter 新增 `version` 与 `whenToUse` 字段。

#### 修复

- 修正 `references/redesign-audit.md` 中对 SKILL.md 章节号的引用（2.4 → 2.5）。

#### 0.1.0 改造对应的 9 个原始问题

| # | 问题 | 对应改动 |
|---|------|---------|
| 1 | 重设计前不盘点现状，项目里预存的不合理（多选菜单有颜色标记、单选菜单没有，而颜色标注本质是文件夹维度功能）未被发现还被延续 | 新增：重设计强制前置 + 三查 + 执行侧盘点与审计段落 |
| 2 | 文档对功能的事实描述是错的（`favorites (star 0–5) with color marks`），错误文档成了设计许可 | 新增：功能类文档事实的处置规则（2.5） |
| 3 | 收藏星级与颜色标记被隐式合并成一个菜单项，未声明未确认 | 新增：功能原子性规则 |
| 4 | 确认摘要把导师自做的设计决定伪装成既定方案，用户审不到这个粒度 | 变更：确认摘要强制二分 |
| 5 | 「复用 X 那一套」被执行者翻译成分叉复制 | 新增：防分叉翻译四件套 |
| 6 | 迭代追加的约束与旧约束冲突，缺少一致性检查步骤 | 新增：迭代模式一致性自检 + 例外条款 |
| 7 | 修正已实现功能的提示词缺少「修正什么 / 保持不动什么」二分结构 | 新增：修正类任务三段结构 |
| 8 | 肯定式完成条件封不住「换个形式加回来」的路径 | 新增：否定式完成条件 |
| 9 | 导师建议产出抽象方法论，对用户无用 | 变更：教学改为原话精确化改写 |

---

## 仓库（不设版本号）

仓库脚手架、全局约定与跨 skill 的改动记录在这里，按日期排列，不设版本号。**约定本身以 [README 的「约定」小节](README.md#约定) 为准**，本节只记它何时变过。

### 2026-09-13

- **版本模型改为按 skill 记录**：本文件由单一版本改为按 skill 分节；每个 skill 的版本条目只保留它自己的能力变更，仓库相关内容移入本节。
- **文档分工确定**：仓库现行约定由 README 的「约定」小节唯一权威说明，本节只记变更、不复述规则。
- `README.md` 的 skill 清单行随 skill 版本同步更新（prompt-mentor 0.3.0、0.4.0、0.4.1 各同步一次）。

### 2026-09-11

- 仓库初始化：确立 `<分类>/<skill>/SKILL.md` 的两层目录约定与三种安装方式（现行说明见 README）。
- 新增 `scripts/validate-skills.mjs` 与 CI 工作流 `.github/workflows/validate-skills.yml`（校验项见 README「校验」）。
