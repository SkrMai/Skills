# 共识包迭代记录

## 基本信息

- 共识包主题：
- 生成日期：
- 任务类型：[Bug / 功能 / 重构 / 优化 / 重设计已有功能 / 修正已交付功能 / 其他]

## 阶段记录

### 探测与审计

- **探测是否发起**：是 / 否
  - 若否，层一三个必要条件逐条判断：
    - 需求描述中所有现状假设均已核实：是 / 否
    - 任务不涉及对已有对象的重新设计：是 / 否
    - 无对照对象需要比对：是 / 否
- **探测执行方式**：AI 直读 / Subagent（数量：）/ 混合 / 未探测（盲区已声明）
- **探测覆盖范围**：
- **盲区**：
- **审计是否有发现**：是 / 否
  - 对称性查发现：
  - 归属查发现：
  - 冲突查发现：
- **裁决结果**：保留 / 移除 / 改 / 待定（逐条对应）

### 放行

- **用户是否放行**：是 / 否
- **放行前修订次数**：
- **放行时仍存在的 `<unverified_assumptions>`**：

## 版本记录

### v1

- **共识包摘要**（`<goal>` + `<requirement_list>` 条目数）：

- **用户反馈**：

- **诊断结果**：
  - [ ] 执行者反复追问 → 共识包缺少约束或上下文
  - [ ] 执行方向错误 → 目标定义或非目标不明确
  - [ ] 做了一半就停 → 停止条件不明确
  - [ ] 做了额外的事 → 非目标未明确或约束不够强
  - [ ] 重设计前未探测现状 → 无现状清单 / 未做合理性审计（对照 `./references/redesign-audit.md`）
  - [ ] 探测判据误用 → 该派 Subagent 却自己读（上下文被稀释），或该自己读却派了 Subagent
  - [ ] 现状里的不合理被原样延续 → 发现了但没让用户裁决，或根本没查
  - [ ] 文档描述当成了设计依据 → 未验证的文档事实被写进 `verified_facts`
  - [ ] 功能被隐式合并 → 需求清单未原子化，或合并未作为设计决策单列
  - [ ] 新增约束与旧约束冲突 → 缺少一致性自检或例外条款
  - [ ] 执行者借小修做大改 → 缺"保持不动"清单或否定式完成条件
  - [ ] 共识包越权指定实现 → 写了文件路径 / 组件名 / 函数名 / `<task>` 段落
  - [ ] 执行阶段发现问题却自行改写共识包 → 未按回退规则回报
  - [ ] 回复末尾又出现"教学 / 导师建议"板块 → 教学应内联在【优化后的需求描述】里（对照 `./references/optimized-requirement.md`）
  - [ ] 【优化后的需求描述】又写成一整段 → 应逐条结构化：一条需求一个块、一个要点一行
  - [ ] 待确认项缺 AI 推荐项，或没排在回复最末 → 用户要翻回去找、要自己猜取舍（对照 `./references/stage1-clarify.md` 第 4 与第 8 节）
  - [ ] 其他：

- **冲突与例外**：
  - 本轮新增约束是否与已有约束/非目标冲突：是 / 否
  - 冲突点：
  - 例外条款（写明允许动哪部分、动到什么程度不算破例）：
  - 修订说明中是否点明"这是本轮新增的例外"：是 / 否

- **修改内容**：

- **修改原因**：

### v2

（同上格式）

## 总结

- 本次迭代中最关键的改进是什么？
- 下次遇到类似任务可以提前做什么来避免这个问题？
- 探测是否真的减少了执行阶段的偏差？哪一类探测最有效？
- 本次是否出现了"导师自己发明的设计决定被当成需求"的情况？
